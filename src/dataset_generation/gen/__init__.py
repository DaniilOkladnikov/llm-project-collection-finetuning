"""Dataset generator package for the pick-and-place DSL.

Turns the task definitions in ``tasks.yaml`` into a fine-tuning dataset of
byte-faithful DSL invocations, driving random scenes from ``scene_generator``.
"""
