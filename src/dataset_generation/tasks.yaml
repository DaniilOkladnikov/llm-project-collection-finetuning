answers: 
  "mustplace": "I must first place what I hold"
  "picked(X, loc)": "I picked up {X} from {loc}"
  "placed(X, loc)": "I placed {X} in {loc}"
  "donthold": "I don't hold anything"
  "released": "I released the gripper"
  "canthelp": "Sorry, but I can't assist you with this task"
  "got(X)": "I got {X} from you"
  "donthold": "I don't hold anything"
  "nospace2place(A)": "There is no empty space in {A}"
  "heldunknown": "I don't know what I'm holding"
  "holding(X)": "I am holding {X}"
  "gripperunknown": "I don't know if the gripper is open or closed"
  "gripperclosed": "The gripper is closed"
  "gripperopen": "The gripper is open"
  "gripperopen": "The gripper is open"
  "atpos(P)": "I am at {P}"
  "posunknown": "I don't know my position"
  "atpos(P)": "I am at {P}"
  "athome": "Yes, I am at home"
  "notathome(P)": "No, I am at {P}"
  "isfull(A)": "{A} is full"
  "notfull(A)": "{A} is not full"
  "isempty(L[A])": "{L[A]} is empty" # Here and further, L[A] denotes list of targets (location or locations collection), all locations are written using coma, no [] in the displayed string, same rules for L[X], list of objects (types)
  "isnonempty(A)": "{A} is not empty"
  "thereis(X,A)": "There is {X} in {A}"
  "nameobjects(A)": "scans[loc] for loc in A"
  "thereis(X,A)": "There is {X} in {A}"
  "thereisno(L[X],L[A])": "There is no {L[X]} in {L[A]}" 
  "locof(X,L[A])": "There is X is in loc for loc in A if scans[loc] = X"
  "thereisno(L[X],L[A])": "There is no {L[X]} in {L[A]}"
  "thereisonly(L[X],L[A])": "There is only {L[X]} in {L[A]}"
  "gotfrom(X,loc)": "I got {X} from {loc}"
  "emptylocs(A)": "MEMORY.scans[A] where value is empty" # comma separated list values
  "received": "Alright, I am holding it now"
  "noneedplace": "No, the gripper is free, you can pick right away"
  "canpick(A)": "Yes, the gripper is free and there is something to pick in {A}"
  "firstemptyat(loc)": "The first empty location is {loc}"
  "lastpicked(X)": "The last thing I picked up was {X}"
  "projectedafter(A)": "Afterwards {A} would contain the remaining objects" # scans[A] minus the found object's location
  "atleastonenonempty(A, B)": "No, at least one of {A} and {B} has something in it"
  "atleastoneempty(A, B)": "Yes, at least one of {A} and {B} is empty"
  "neitherempty(A, B)": "No, both {A} and {B} have something in them"

tasks:
  1:
    prompts: ["Pick up {X} from {A}"]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      else:
        observe A
      remember loc = first in MEMORY.scans[A] where value is X
      if loc is not none:
        pick from loc
        answer_2: picked(X, loc) [2]
      else:
        answer_3: thereisno([X],[A]) [2]
      "
  2:
    prompts: ["Close gripper, I am giving you {X}"]
    program:
      "
      check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      else:
        get X from user 
        answer_2: got(X) [2]
      "
  3:
    prompts: ["Release what you hold"]
    program:
      "
      check gripper
      if MEMORY.gripper is open:
        answer_1: donthold [2]
      else:
        open gripper
        answer_2: released [2]
      "
  4:
    prompts: ["You are holding {X}. Place it into {A}"]
    state_change: 
      - gripper: closed
      - held: X
    program:
      "
      observe A
      remember loc = first in MEMORY.scans[A] where value is empty
      if loc is not none:
        place in loc
        answer_2: placed(X, loc) [2]
      else:
        answer_3: nospace2place(A) [2]
      "
  5:
    prompts: ["Am I holding {X}?", "What are you holding?", "Do you know what you are holding?"]
    program:
      "answer_1:
        if MEMORY.held is unknown: heldunknown [2]
        else: holding(MEMORY.held) [2]"
  6:
    prompts: ["Is your gripper free?"]
    program:
      "answer_1:
        if MEMORY.gripper is unknown: gripperunknown [2]
        elif MEMORY.gripper is closed: gripperclosed [2]
        else: gripperopen [2]"
  7:
    prompts: ["What position are you at?"]
    program:
      "answer_1:
        if MEMORY.position is unknown: posunknown [2]
        else: atpos(MEMORY.position) [2]"
  8:
    prompts: ["Go to {P position}", "Move to {P}"]
    program:
      "go to P
      answer_1: atpos(P) [2]
      " # Note, P in the program is literally what user said, can be different from real position name, P in the answer in the actual position name saved in memory by parse user command and where the robot actually is.
  9:
    prompts: ["Go to the {observation point for A}"]
    program:
      "go to observation of A
      answer_1: atpos(observation point for A) [2]
      " # Note, here and further observation point for A in the program is literally what user said, observation point for A in the answer in the actual position name saved in memory by parse user command and where the robot actually is.
  10:
    prompts: ["Visit the observation points for {A} and then {B}"]
    program:
      "go to observation of A
      go to observation of B
      answer_1: atpos(observation of B) [2]" # observation of B in the answer is the actual position name
  11:
    prompts: ["Find empty space in A and go to position to pickup from there. And check if gripper is open", "Get ready to pick from empty in A"]
    program:
      "observe A
      check gripper
      if MEMORY.gripper is open:
        answer_1: mustplace [2]
      remember loc = first in MEMORY.scans[A] where value is empty
      if loc is not none:
        go to pick MEMORY.scans[loc] from loc
        answer_2: atpos(loc) [2]
      else:
        answer_3: isfull(A) [2]
      "
  12:
    prompts: ["Prepare to pick something from A", "Find an object in A prepare to pick it"]
    program:
      "observe A
      remember loc = first in MEMORY.scans[A] where value is not empty
      if loc is not none:
        go to pick MEMORY.scans[loc] from loc
        answer_1: atpos(loc) [2]
      else:
        answer_2: isempty([A]) [2]
      "
  13:
    prompts: ["What is in A?"]
    program:
      "observe A
      answer_1:
        if A empty: isempty([A]) [2]
        else: nameobjects(A) [2]"
  14:
    prompts: ["Is there an X in A?", "Check whether A still has an X."]
    program:
      "observe A
      answer_1:
        if X in A : thereis(X,A) [2]
        else: thereisno([X],[A]) [2]"
  15:
    prompts: ["Are there any empty slots in A?"]
    program:
      "observe A
      answer_1:
        if A empty: isempty([A]) [2]
        else: isnonempty(A) [2]"
  16:
    prompts: ["Which slot in A has X?", "Find X in A."]
    program:
      "observe A
      answer_1:
        if X in A: locof(X,[A]) [2]
        else: thereisno([X],[A]) [2]"
  17:
    prompts: ["Which has X, A or B?"]
    program:
      "observe A
      observe B
      answer_1:
        if X in A or X in B: locof(X,[A,B]) [2]
        else: thereisno(X,[A,B]) [2]"
  18:
    prompts: ["Pick up whatever is in A"]
    program:
      "check gripper
       if gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is not empty
      if loc is not none:
        pick from loc
        answer_2: picked(MEMORY.scans[loc], loc) [2]
      else:
        answer_3: isempty([A]) [2]
      "
  19:
    prompts: ["Pick up whatever is in A. If A is empty, pick from B instead."]
    program:
      "check gripper
       if gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is not empty
      if loc is not none:
        pick from loc
        answer_2: picked(X, loc) [2]
      observe B
      remember loc = first in MEMORY.scans[B] where value is not empty
      if loc is not none:
        place in loc
        answer_3: picked(X, loc) [2]
      else:
        answer_4: isempty([A, B]) [2]
      "
  20:
    prompts: ["Pick up X or W from A."]
    program:
      "check gripper
      if gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is [X, W]
      if loc is not none:
        pick from loc
        answer_2: picked(X, loc) [2]
      else:
        answer_3: 
          if A empty: isempty(A) [2]
          else: thereisno([X, W], [A]) [2]
      "
  21:
    prompts: ["Pick up whatever is at A, as long as it is not a W"]
    program:
      "check gripper
      if gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is not W
      if loc is not none:
        pick from loc
        answer_2: picked(X, loc) [2]
      else:
        answer_3: 
          if A empty: isempty(A) [2]
          else: thereisonly([W], [A]) [2]
      "
  22:
    prompts: ["Get X from A and come back to where you started"]
    program:
      "check gripper
      if gripper is closed:
        answer_1: mustplace [2]
      remember original position = check position
      observe A
      remember loc = first in MEMORY.scans[A] where value is X
      if loc is not none:
        pick from loc
        go to MEMORY.original position
        answer_2: picked(X, loc). atpos(MEMORY.original position) [2]
      else:
        answer_3: thereisno([X], [A]) [2]"
  23:
    prompts: ["Place what you're holding somewhere."]
    program:
      "check gripper
      if gripper is open:
        answer_1: donthold [2]
      if MEMORY.held is unknown:
        answer_2: heldunknown [2]
      observe everything
      remember loc = first in MEMORY.scans where value is empty
      if loc is not none:
        place in loc
        answer_3: placed(X, loc) [2]
      else:
        answer_4: nospace2place() [2]
      " # nospace2place with empty argument means at all (in the whole scene)
  24:
    prompts: ["Place what you are holding at A"]
    program:
      "check gripper
      if gripper is open:
        answer_1: donthold [2]
      if MEMORY.held is unknown:
        answer_2: heldunknown [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is empty
      if loc is not none:
        place in loc
        answer_3: placed(X, loc) [2]
      else:
        answer_4: nospace2place(A) [2]
      "
  25:
    prompts: ["Move X from A to B"]
    program:
      "
      check gripper
      if gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is X
      if loc is not none:
        pick from loc
      else:
        answer_3: thereisno([X], [A]) [2]
      observe B
      remember loc = first in MEMORY.scans[B] where value is empty
      if loc is not none:
        place in loc
        answer_2: placed(X, loc) [2]
      else:
        answer_4: nospace2place(B) [2]
      "
  26:
    prompts: ["OK, put it back {where you got it}", "OK, put it back where you got it."]
    comes only after: [picked(X, loc)] # {where you got it} = loc
    program:
      "
      check gripper
      if gripper is open:
        answer_1: donthold [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is X
      if loc is not none:
        place in loc
        answer_2: placed(X, loc) [2]
      else:
        answer_3: nospace2place(A) [2]
      "
  27:
    prompts: ["Find and pick up an X"]
    program:
      "check gripper
      if gripper is closed:
        answer_1: mustplace [2]
      observe everything
      remember loc = first in MEMORY.scans where value is X
      if loc is not none:
        pick from loc
        answer_2: picked(MEMORY.scans[loc], loc) [2]
      else:
        answer_3: thereisno([X]) [2]
      " # thereisno without location means everywhere
  28:
    prompts: ["Where did you get X from?"]
    comes only after: [picked(X, loc)] # {where you got it} = loc
    program:
      "answer_1: gotfrom(X,loc) [2]"
  29:
    prompts: ["If A is not empty, pick something from A. Otherwise, pick something from B"]
    program:
      "observe A
      remember loc = first in MEMORY.scans[A] where value is not [empty]
      if loc is not none:
        pick from loc
        answer_1: picked(X, loc) [2]
      observe B
      remember loc = first in MEMORY.scans[B] where value is not [empty]
      if loc is not none:
        pick from loc
        answer_2: picked(X, loc) [2]
      else:
        answer_3: isempty([A, B]) [2]
      "
  30:
    prompts: ["Go to A, see what's there, pick it up if that's an X, then come back"]
    program:
      "remember original position = check position
      observe A
      remember loc = first in MEMORY.scans[A] where value is X
      if loc is not none:
        check gripper
        if gripper is closed:
          answer_1: mustplace [2]
        else:
          pick from loc
          go to MEMORY.original position
          answer_2: picked(X, loc). atpos(MEMORY.original position) [2]
      else:
        go to MEMORY.original position
        answer_3: thereisno([X], [A]). atpos(MEMORY.original position) [2]
      "
  31:
    prompts: ["Pick up whatever is in A and bring it back."]
    program:
      "check gripper
      if gripper is closed:
        answer_1: mustplace [2]
      remember original position = check position
      observe A
      remember loc = first in MEMORY.scans[A] where value is not empty
      if loc is not none:
        pick from loc
        go to MEMORY.original position
        answer_2: picked(MEMORY.scans[loc], loc). atpos(MEMORY.original position) [2]
      else:
        answer_3: isempty([A]) [2]
      "
  32:
    prompts: ["Place {X} somewhere", "Put {X} down anywhere"]
    program:
      "check gripper
      if gripper is open:
        answer_1: donthold [2]
      if MEMORY.held is unknown:
        answer_2: heldunknown [2]
      observe everything
      remember loc = first in MEMORY.scans where value is empty
      if loc is not none:
        place in loc
        answer_3: placed(X, loc) [2]
      else:
        answer_4: nospace2place() [2]
      "
  33:
    prompts: ["Place {X}, preferring {A}. If {A} is full, find any empty spot"]
    program:
      "check gripper
      if gripper is open:
        answer_1: donthold [2]
      if MEMORY.held is unknown:
        answer_2: heldunknown [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is empty
      if loc is not none:
        place in loc
        answer_3: placed(X, loc) [2]
      observe everything
      remember loc = first in MEMORY.scans where value is empty
      if loc is not none:
        place in loc
        answer_4: placed(X, loc) [2]
      answer_5: nospace2place() [2]
      "
  34:
    prompts: ["You are holding {X}. Place it somewhere, but not in {A}"]
    state_change:
      - gripper: closed
      - held: X
    program:
      "observe everything
      remember loc = first in MEMORY.scans[not A] where value is empty
      if loc is not none:
        place in loc
        answer_1: placed(X, loc) [2]
      else:
        answer_2: nospace2place() [2]
      "
  35:
    prompts: ["You are holding {X}. Place it at {A}, otherwise try {B}"]
    state_change:
      - gripper: closed
      - held: X
    program:
      "observe A
      remember loc = first in MEMORY.scans[A] where value is empty
      if loc is not none:
        place in loc
        answer_1: placed(X, loc) [2]
      observe B
      remember loc = first in MEMORY.scans[B] where value is empty
      if loc is not none:
        place in loc
        answer_2: placed(X, loc) [2]
      else:
        answer_3: nospace2place([A, B]) [2]
      "
  36:
    prompts: ["You are holding {X}. Place it at {A}, only if {A} is completely empty"]
    state_change:
      - gripper: closed
      - held: X
    program:
      "observe A
      remember nonemptyloc = first in MEMORY.scans[A] where value is not empty
      if nonemptyloc is none:
        remember loc = first in MEMORY.scans[A]
        place in loc
        answer_1: placed(X, loc) [2]
      else:
        answer_2: isnonempty(A) [2]
      "
  37:
    prompts: ["You are holding {X}. Place it at {A}; if {A} has a {W} in it, place it at {B} instead"]
    state_change:
      - gripper: closed
      - held: X
    program:
      "observe A
      remember wloc = first in MEMORY.scans[A] where value is W
      if wloc is none:
        remember loc = first in MEMORY.scans[A] where value is empty
        if loc is not none:
          place in loc
          answer_1: placed(X, loc) [2]
        else:
          answer_2: nospace2place(A) [2]
      else:
        observe B
        remember loc = first in MEMORY.scans[B] where value is empty
        if loc is not none:
          place in loc
          answer_3: placed(X, loc) [2]
        else:
          answer_4: nospace2place(B) [2]
      "
  38:
    prompts: ["Place what you hold somewhere, but not in {A}"]
    program:
      "
      check gripper
      if gripper is open:
        answer_1: donthold [2]
      if MEMORY.held is unknown:
        answer_2: heldunknown [2]
      observe everything
      remember loc = first in MEMORY.scans[not A] where value is empty
      if loc is not none:
        place in loc
        answer_3: placed(X, loc) [2]
      else:
        answer_4: nospace2place() [2]
      "
  39:
    prompts: ["Place what you hold in {A}, otherwise try {B}"]
    state_change:
    program:
      "check gripper
      if gripper is open:
        answer_1: donthold [2]
      if MEMORY.held is unknown:
        answer_2: heldunknown [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is empty
      if loc is not none:
        place in loc
        answer_1: placed(X, loc) [2]
      observe B
      remember loc = first in MEMORY.scans[B] where value is empty
      if loc is not none:
        place in loc
        answer_2: placed(X, loc) [2]
      else:
        answer_3: nospace2place([A, B]) [2]
      "
  40:
    prompts: ["Place the held object at {A}, only if {A} is completely empty"]
    program:
      "check gripper
      if gripper is open:
        answer_1: donthold [2]
      if MEMORY.held is unknown:
        answer_2: heldunknown [2]
      observe A
      remember nonemptyloc = first in MEMORY.scans[A] where value is not empty
      if nonemptyloc is none:
        remember loc = first in MEMORY.scans[A]
        place in loc
        answer_1: placed(X, loc) [2]
      else:
        answer_2: isnonempty(A) [2]
      "
  41:
    prompts: ["Place the thing in your gripper in {A}; if {A} has a {W} in it, place it at {B} instead"]
    program:
      "check gripper
      if gripper is open:
        answer_1: donthold [2]
      if MEMORY.held is unknown:
        answer_2: heldunknown [2]
      observe A
      remember wloc = first in MEMORY.scans[A] where value is W
      if wloc is none:
        remember loc = first in MEMORY.scans[A] where value is empty
        if loc is not none:
          place in loc
          answer_1: placed(X, loc) [2]
        else:
          answer_2: nospace2place(A) [2]
      else:
        observe B
        remember loc = first in MEMORY.scans[B] where value is empty
        if loc is not none:
          place in loc
          answer_3: placed(X, loc) [2]
        else:
          answer_4: nospace2place(B) [2]
      "
  42:
    prompts: ["Move {X} from {A} to {B}; if {B} is full, put {X} back in {A}"]
    program:
      "
      check gripper
      if gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember srcloc = first in MEMORY.scans[A] where value is X
      if srcloc is none:
        answer_2: thereisno([X], [A]) [2]
      else:
        pick from srcloc
        observe B
        remember loc = first in MEMORY.scans[B] where value is empty
        if loc is not none:
          place in loc
          answer_3: placed(X, loc) [2]
        else:
          place in srcloc
          answer_4: isfull(B). placed(X, srcloc) [2]
      "
  43:
    prompts: ["Pick whatever is at {A} and place it in {B}"]
    program:
      "
      check gripper
      if gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember srcloc = first in MEMORY.scans[A] where value is not empty
      if srcloc is not none:
        pick from srcloc
      else:
        answer_2: isempty([A]) [2]
      observe B
      remember loc = first in MEMORY.scans[B] where value is empty
      if loc is not none:
        place in loc
        answer_3: placed(X, loc) [2]
      else:
        answer_4: nospace2place(B) [2]
      "
  44:
    prompts: ["Check what is at {A}; if there is an {X}, pick it, otherwise tell me what is there"]
    program:
      "
      observe A
      remember loc = first in MEMORY.scans[A] where value is X
      if loc is not none:
        check gripper
        if gripper is closed:
          answer_1: mustplace [2]
        else:
          pick from loc
          answer_2: picked(X, loc) [2]
      else:
        answer_3:
          if A empty: isempty([A]) [2]
          else: nameobjects(A) [2]
      "
  45:
    prompts: ["Which locations in A are empty right now?"]
    program:
      "observe A
      answer_1:
        if A empty: isempty([A]) [2]
        else: emptylocs(A) [2]"
  46:
    prompts: ["If there is an X in A, move it to B"]
    program:
      "
      check gripper
      if gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is X
      if loc is not none:
        pick from loc
      else:
        answer_2: thereisno([X], [A]) [2]
      observe B
      remember loc = first in MEMORY.scans[B] where value is empty
      if loc is not none:
        place in loc
        answer_2: placed(X, loc) [2]
      else:
        answer_3: nospace2place(B) [2]
      "
  47:
    prompts: ["Is A empty? If so, go to P; if not, tell me what is there"]
    program:
      "observe A
      if A empty:
        go to P
        answer_1: atpos(P) [2]
      else:
        answer_2: nameobjects(A) [2]
      "
  48:
    prompts: ["You are holding {X}. Place it at {A}; report whether {A} is then full"]
    state_change:
      - gripper: closed
      - held: X
    program:
      "observe A
      remember loc = first in MEMORY.scans[A] where value is empty
      if loc is not none:
        place in loc
      else:
        answer_1: nospace2place(A) [2]
      remember nextloc = first in MEMORY.scans[A] where value is empty
      if nextloc is none:
        answer_2: placed(X, loc). isfull(A) [2]
      else:
        answer_3: placed(X, loc). notfull(A) [2]
      "
  49:
    prompts: ["Put held entity into {A}; report whether {A} is then full"]
    program:
      "check gripper
      if gripper is open:
        answer_1: donthold [2]
      if MEMORY.held is unknown:
        answer_2: heldunknown [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is empty
      if loc is not none:
        place in loc
      else:
        answer_3: nospace2place(A) [2]
      remember nextloc = first in MEMORY.scans[A] where value is empty
      if nextloc is none:
        answer_4: placed(X, loc). isfull(A) [2]
      else:
        answer_5: placed(X, loc). notfull(A) [2]
      "
  50:
    prompts: ["Check A and B; pick the X from whichever has it"]
    program:
      "check gripper
      if gripper is closed:
        answer_1: mustplace [2]
      observe A
      observe B
      remember loc = first in MEMORY.scans[A and B] where value is X
      if loc is not none:
        pick from loc
        answer_2: picked(X, loc) [2]
      else:
        answer_3: thereisno([X], [A, B]) [2]
      "
  51:
    prompts: ["You are holding something. If it is an {X} place it at {A}; if it is a {W} place it at {B}"]
    program:
      "check gripper
      if gripper is open:
        answer_1: donthold [2]
      if MEMORY.held is unknown:
        answer_2: heldunknown [2]
      if MEMORY.held is X:
        observe A
        remember loc = first in MEMORY.scans[A] where value is empty
        if loc is not none:
          place in loc
          answer_3: placed(X, loc) [2]
        else:
          answer_4: nospace2place(A) [2]
      elif MEMORY.held is W:
        observe B
        remember loc = first in MEMORY.scans[B] where value is empty
        if loc is not none:
          place in loc
          answer_5: placed(W, loc) [2]
        else:
          answer_6: nospace2place(B) [2]
      "
  52:
    prompts: ["You are holding {X}. Place it somewhere, then go to {P}"]
    state_change:
      - gripper: closed
      - held: X
    program:
      "observe everything
      remember loc = first in MEMORY.scans where value is empty
      if loc is not none:
        place in loc
        go to P
        answer_1: placed(X, loc). atpos(P) [2]
      else:
        answer_2: nospace2place() [2]
      "
  53:
    prompts: ["You are holding {W}. Drop it at {A}, then bring an {X} back to where you started"]
    state_change:
      - gripper: closed
      - held: W
    program:
      "remember original position = check position
      observe A
      remember loc = first in MEMORY.scans[A] where value is empty
      if loc is not none:
        place in loc
        answer_1: placed(W, loc) [2]
      else:
        answer_2: nospace2place(A) [2]
      observe everything
      remember loc = first in MEMORY.scans where value is X
      if loc is not none:
        pick from loc
        go to MEMORY.original position
        answer_3: picked(X, loc). atpos(MEMORY.original position) [2]
      else:
        answer_4: thereisno([X]) [2]
      "
  54:
    prompts: ["Place held item somewhere, then go to {P}"]
    program:
      "check gripper
      if gripper is open:
        answer_1: donthold [2]
      if MEMORY.held is unknown:
        answer_2: heldunknown [2]
      observe everything
      remember loc = first in MEMORY.scans where value is empty
      if loc is not none:
        place in loc
        go to P
        answer_3: placed(X, loc). atpos(P) [2]
      else:
        answer_4: nospace2place() [2]
      "
  55:
    prompts: ["Drop what hold at {A}, then bring an {X} back to where you started"]
    program:
      "check gripper
      if gripper is open:
        answer_1: donthold [2]
      if MEMORY.held is unknown:
        answer_2: heldunknown [2]
      remember original position = check position
      observe A
      remember loc = first in MEMORY.scans[A] where value is empty
      if loc is not none:
        place in loc
        answer_3: placed(W, loc) [2]
      else:
        answer_4: nospace2place(A) [2]
      observe everything
      remember loc = first in MEMORY.scans where value is X
      if loc is not none:
        pick from loc
        go to MEMORY.original position
        answer_5: picked(X, loc). atpos(MEMORY.original position) [2]
      else:
        answer_6: thereisno([X]) [2]
      "
  56:
    prompts: ["If A has an X, pick it; otherwise go home"]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      else:
        observe A
      remember loc = first in MEMORY.scans[A] where value is X
      if loc is not none:
        pick from loc
        answer_2: picked(X, loc) [2]
      else:
        go to home
        answer_3: thereisno([X], [A]). atpos(home) [2]
      "
  57:
    prompts: ["Am I at home?"]
    program:
      "answer_1":
        if MEMORY.position is unknown: "posunknown" [2]
        elif MEMORY.position is home: "athome" [2]
        else: "notathome(MEMORY.position)" [2]
  58:
    prompts: ["If you're holding something, place it at A; if not, pick an X from A."]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        observe A
        remember loc = first in MEMORY.scans[A] where value is empty
        if loc is not none:
          place in loc
          answer_1: placed(MEMORY.held, loc) [2]
        else:
          answer_2: nospace2place(A) [2]
      else:
        observe A
        remember loc = first in MEMORY.scans[A] where value is X
        if loc is not none:
          pick from loc
          answer_3: picked(X, loc) [2]
        else:
          answer_4: thereisno([X], [A]) [2]
      "
  59:
    prompts: ["If A has nothing in it, put what you're holding there."]
    program:
      "check gripper
      if MEMORY.gripper is open:
        answer_1: donthold [2]
      if MEMORY.held is unknown:
        answer_2: heldunknown [2]
      observe A
      remember occupied = first in MEMORY.scans[A] where value is not empty
      if occupied is none:
        remember loc = first in MEMORY.scans[A] where value is empty
        place in loc
        answer_3: placed(MEMORY.held, loc) [2]
      else:
        answer_4: isnonempty(A) [2]
      "
  60:
    prompts: ["Pick from A, but only if B is empty."]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      observe B
      remember occupied = first in MEMORY.scans[B] where value is not empty
      if occupied is not none:
        answer_2: isnonempty(B) [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is not empty
      if loc is not none:
        pick from loc
        answer_3: picked(MEMORY.scans[loc], loc) [2]
      else:
        answer_4: isempty([A]) [2]
      "
  61:
    prompts: ["If A is full, tell me what's in B."]
    program:
      "observe A
      remember emptyloc = first in MEMORY.scans[A] where value is empty
      if emptyloc is none:
        observe B
        answer_1:
          if B empty: isempty([B]) [2]
          else: nameobjects(B) [2]
      else:
        answer_2: notfull(A) [2]
      "
  62:
    prompts: ["Did the last pick actually work? Check the gripper."]
    program:
      "check gripper
      answer_1:
        if MEMORY.gripper is closed: gripperclosed [2]
        else: gripperopen [2]"
  63:
    prompts: ["You just placed X in A - confirm it's really there."]
    program:
      "observe A
      remember loc = first in MEMORY.scans[A] where value is X
      answer_1:
        if X in A: thereis(X, A) [2]
        else: thereisno([X], [A]) [2]"
  64:
    prompts: ["Pick an X from A, B, or C - whichever has one."]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      observe A
      observe B
      observe C
      remember loc = first in MEMORY.scans[A, B, C] where value is X
      if loc is not none:
        pick from loc
        answer_2: picked(X, loc) [2]
      else:
        answer_3: thereisno([X], [A, B, C]) [2]
      "
  65:
    prompts: ["Move the X from A to whichever of B or C has room."]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember src = first in MEMORY.scans[A] where value is X
      if src is none:
        answer_2: thereisno([X], [A]) [2]
      observe B
      remember loc = first in MEMORY.scans[B] where value is empty
      if loc is not none:
        pick from src
        place in loc
        answer_3: placed(X, loc) [2]
      observe C
      remember loc = first in MEMORY.scans[C] where value is empty
      if loc is not none:
        pick from src
        place in loc
        answer_4: placed(X, loc) [2]
      else:
        answer_5: nospace2place([B, C]). placed(X, src) [2]
      "
  66:
    prompts: ["Verify the gripper actually closed on something."]
    program:
      "check gripper
      answer_1:
        if MEMORY.gripper is closed: gripperclosed [2]
        else: gripperopen [2]"
  67:
    prompts: ["After you place this, go check B."]
    program:
      "check gripper
      if MEMORY.gripper is open:
        answer_1: donthold [2]
      if MEMORY.held is unknown:
        answer_2: heldunknown [2]
      observe everything
      remember loc = first in MEMORY.scans where value is empty
      if loc is none:
        answer_3: nospace2place() [2]
      place in loc
      observe B
      answer_4:
        if B empty: placed(MEMORY.held, loc). isempty([B]) [2]
        else: placed(MEMORY.held, loc). nameobjects(B) [2]
      "
  68:
    prompts: ["What was the last thing you picked up?"]
    comes only after: [picked(X, loc)]
    program:
      "answer_1: lastpicked(X) [2]"
  69:
    prompts: ["You are holding X."]
    state_change:
      - gripper: closed
      - held: X
    program:
      "answer_1: holding(X) [2]"
  70:
    prompts: ["You are holding an X. Put this one to A and pick up another one"]
    state_change:
      - gripper: closed
      - held: X
    program:
      "observe A
      remember loc = first in MEMORY.scans[A] where value is empty
      if loc is not none:
        place in loc
        answer_1: placed(X, loc) [2]
      else:
        answer_2: nospace2place(A) [2]
      observe everything
      remember loc = first in MEMORY.scans where value is X
      if loc is not none:
        pick from loc
        answer_3: picked(X, loc) [2]
      else:
        answer_4: thereisno([X]) [2]
      "
  71:
    prompts: ["Are you sure? Check again"]
    comes only after: '[thereis(X, A), thereisno([X], [A])]' # X, A from the previous query
    program:
      "observe A
      answer_1:
        if X in A: thereis(X, A) [2]
        else: thereisno([X], [A]) [2]"
  72:
    prompts: ["Place X at A but confirm it's empty in the first place."]
    state_change:
      - gripper: closed
      - held: X
    program:
      "observe A
      remember occupied = first in MEMORY.scans[A] where value is not empty
      if occupied is none:
        remember loc = first in MEMORY.scans[A] where value is empty
        place in loc
        answer_1: placed(X, loc) [2]
      else:
        answer_2: isnonempty(A) [2]
      "
  73:
    prompts: ["Tell me the first empty location in A, but don't place anything."]
    program:
      "observe A
      answer_1:
        if loc is not none: firstemptyat(loc) [2]
        else: isfull(A) [2]"
  74:
    prompts: ["Tell me which locations in A are empty and which are full."]
    program:
      "observe A
      answer_1:
        if A empty: isempty([A]) [2]
        else: emptylocs(A). nameobjects(A) [2]"
  75:
    prompts: ["Is there room for what I'm holding in A?"]
    program:
      "check gripper
      if MEMORY.gripper is open:
        answer_1: donthold [2]
      observe A
      answer_2:
        if A is not full: notfull(A) [2]
        else: isfull(A) [2]"
  76:
    prompts: ["Do I need to place what I'm holding before I can pick from A?"]
    program:
      "check gripper
      answer_1:
        if MEMORY.gripper is closed: mustplace [2]
        else: noneedplace [2]"
  77:
    prompts: ["Make sure A has an empty slot"]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is empty
      if loc is not none:
        answer_2: notfull(A) [2]
      remember src = first in MEMORY.scans[A] where value is not empty
      observe everything
      remember dest = first in MEMORY.scans[not A] where value is empty
      if dest is not none:
        pick from src
        place in dest
        answer_3: placed(MEMORY.held, dest) [2]
      else:
        answer_4: isfull(A). nospace2place() [2]
      "
  78:
    prompts: ["If A is already empty, say so; otherwise tell me what's blocking it."]
    program:
      "observe A
      answer_1:
        if A empty: isempty([A]) [2]
        else: nameobjects(A) [2]"
  79:
    prompts: ["Where are all the X right now?", "List all the X you can find"]
    program:
      "observe everything
      answer_1:
        if loc is none: thereisno([X]) [2]
        else: locof(X, everything) [2]"
  80:
    prompts: ["I just added something to A, look again."]
    program:
      "observe A
      answer_1:
        if A empty: isempty([A]) [2]
        else: nameobjects(A) [2]" # re-scan forced: the scene changed since the last observation
  81:
    prompts: ["Something fell. Recheck everything."]
    program:
      "observe everything
      answer_1: nameobjects(everything) [2]" # re-scan forced: the scene changed since the last observation
  82:
    prompts: ["Find X. Look in A,B,C."]
    program:
      "observe A
      observe B
      observe C
      remember loc = first in MEMORY.scans[A, B, C] where value is X
      answer_1:
        if loc is not none: locof(X, [A, B, C]) [2]
        else: thereisno([X], [A, B, C]) [2]"
  83:
    prompts: ["Before you move it, tell me what A will look like afterward."]
    comes only after: '[thereis(X, A), locof(X, [A])]' # a find that located X in exactly A
    program:
      "observe A
      answer_1: projectedafter(A) [2]"
  84:
    prompts: ["Check A: if it has an X, tell me where B could hold it; if not, just say so."]
    program:
      "observe A
      remember loc = first in MEMORY.scans[A] where value is X
      if loc is not none:
        observe B
        remember dest = first in MEMORY.scans[B] where value is empty
        answer_1:
          if dest is not none: firstemptyat(dest) [2]
          else: isfull(B) [2]
      else:
        answer_2: thereisno([X], [A]) [2]
      "
  85:
    prompts: ["Is what I'm holding also present somewhere in A?"]
    program:
      "check gripper
      if MEMORY.gripper is open:
        answer_1: donthold [2]
      if MEMORY.held is unknown:
        answer_2: heldunknown [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is MEMORY.held
      answer_3:
        if loc is not none: thereis(MEMORY.held, A) [2]
        else: thereisno([MEMORY.held], [A]) [2]"
  86:
    prompts: ["Is A empty?"]
    program:
      "observe A
      answer_1:
        if A empty: isempty([A]) [2]
        else: isnonempty(A) [2]"
  87:
    prompts: ["Is A full?"]
    program:
      "observe A
      answer_1:
        if A full: isfull(A) [2]
        else: notfull(A) [2]"
  88:
    prompts: ["Is there anything other than X in A?"]
    program:
      "observe A
      answer_1:
        if first in MEMORY.scans[A] where value is not [X, empty] is not none: thereis(MEMORY.scans[other], A) [2]
        else: thereisonly([X], [A]) [2]"
  89:
    prompts: ["Are A and B both empty?"]
    program:
      "observe A
      observe B
      answer_1:
        if A empty and B empty: isempty([A, B]) [2]
        else: atleastonenonempty(A, B) [2]"
  90:
    prompts: ["Is either A or B empty?"]
    program:
      "observe A
      observe B
      answer_1:
        if A empty or B empty: atleastoneempty(A, B) [2]
        else: neitherempty(A, B) [2]"
  91:
    prompts: ["Go to P, then come back to where you started"]
    program:
      "remember original position = check position
      go to P
      go to MEMORY.original position
      answer_1: atpos(MEMORY.original position) [2]"
  92:
    prompts: ["Pick up an object at A that is not an X"]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is not [X, empty]
      if loc is not none:
        pick from loc
        answer_2: picked(MEMORY.scans[loc], loc) [2]
      else:
        answer_3:
          if A empty: isempty([A]) [2]
          else: thereisonly([X], [A]) [2]
      "
  93:
    prompts: ["Pick from A the same kind of object that is in B"]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      observe A
      observe B
      remember refloc = first in MEMORY.scans[B] where value is not empty
      if refloc is none:
        answer_2: isempty([B]) [2]
      remember reftype = MEMORY.scans[refloc]
      remember loc = first in MEMORY.scans[A] where value is reftype
      if loc is not none:
        pick from loc
        answer_3: picked(reftype, loc) [2]
      else:
        answer_4: thereisno([reftype], [A]) [2]
      "
  94:
    prompts: ["Pick from A only if everything there is an X"]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember occupied = first in MEMORY.scans[A] where value is not empty
      if occupied is none:
        answer_2: isempty([A]) [2]
      remember other = first in MEMORY.scans[A] where value is not [X, empty]
      if other is none:
        remember loc = first in MEMORY.scans[A] where value is X
        pick from loc
        answer_3: picked(X, loc) [2]
      else:
        answer_4: thereis(MEMORY.scans[other], A) [2]
      "
  95:
    prompts: ["Check A; if it has an X, pick something from B"]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember xloc = first in MEMORY.scans[A] where value is X
      if xloc is none:
        answer_2: thereisno([X], [A]) [2]
      observe B
      remember loc = first in MEMORY.scans[B] where value is not empty
      if loc is not none:
        pick from loc
        answer_3: picked(MEMORY.scans[loc], loc) [2]
      else:
        answer_4: isempty([B]) [2]
      "
  96:
    prompts: ["You are holding X. Place it at A; if A already has an X, place it at B instead"]
    state_change:
      - gripper: closed
      - held: X
    program:
      "observe A
      remember xloc = first in MEMORY.scans[A] where value is X
      if xloc is none:
        remember loc = first in MEMORY.scans[A] where value is empty
        if loc is not none:
          place in loc
          answer_1: placed(X, loc) [2]
        else:
          answer_2: nospace2place(A) [2]
      else:
        observe B
        remember loc = first in MEMORY.scans[B] where value is empty
        if loc is not none:
          place in loc
          answer_3: placed(X, loc) [2]
        else:
          answer_4: nospace2place(B) [2]
      "
  97:
    prompts: ["You are holding X. Place it at A, unless A already has an X"]
    state_change:
      - gripper: closed
      - held: X
    program:
      "observe A
      remember xloc = first in MEMORY.scans[A] where value is X
      if xloc is not none:
        answer_1: thereis(X, A) [2]
      remember loc = first in MEMORY.scans[A] where value is empty
      if loc is not none:
        place in loc
        answer_2: placed(X, loc) [2]
      else:
        answer_3: nospace2place(A) [2]
      "
  98:
    prompts: ["You are holding W. Put it down first, then pick up an X"]
    state_change:
      - gripper: closed
      - held: W
    program:
      "observe everything
      remember loc = first in MEMORY.scans where value is empty
      if loc is none:
        answer_1: nospace2place() [2]
      place in loc
      remember xloc = first in MEMORY.scans where value is X
      if xloc is not none:
        pick from xloc
        answer_2: placed(W, loc). picked(X, xloc) [2]
      else:
        answer_3: placed(W, loc). thereisno([X]) [2]
      "
  99:
    prompts: ["You are holding X. Place it at A; if A is full, keep holding it"]
    state_change:
      - gripper: closed
      - held: X
    program:
      "observe A
      remember loc = first in MEMORY.scans[A] where value is empty
      if loc is not none:
        place in loc
        answer_1: placed(X, loc) [2]
      else:
        answer_2: isfull(A). holding(X) [2]
      "
  100:
    prompts: ["Find any empty location and report it"]
    program:
      "observe everything
      answer_1:
        if first in MEMORY.scans where value is empty is not none: firstemptyat(loc) [2]
        else: nospace2place() [2]"
  101:
    prompts: ["Find an X; if it is at A, move it to B, otherwise leave it"]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      observe A
      observe B
      remember src = first in MEMORY.scans[A] where value is X
      if src is none:
        answer_2: thereisno([X], [A]) [2]
      remember dest = first in MEMORY.scans[B] where value is empty
      if dest is none:
        answer_3: nospace2place(B) [2]
      pick from src
      place in dest
      answer_4: picked(X, src). placed(X, dest) [2]
      "
  102:
    prompts: ["Can I now pick something from A?"]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is not empty
      answer_2:
        if loc is not none: canpick(A) [2]
        else: isempty([A]) [2]"
  103:
    prompts: ["Pick X from A, then tell me what A has left"]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is X
      if loc is not none:
        pick from loc
        answer_2: picked(X, loc). nameobjects(A) [2]
      else:
        answer_3: thereisno([X], [A]) [2]
      "
  104:
    prompts: ["Tell me what is at A, then pick up the X from there"]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      observe A
      remember loc = first in MEMORY.scans[A] where value is X
      if loc is not none:
        pick from loc
        answer_2: nameobjects(A). picked(X, loc) [2]
      else:
        answer_3: nameobjects(A). thereisno([X], [A]) [2]
      " # nameobjects(A) reports A after the pick (what is left)
  105:
    prompts: ["Ensure there is an X at A"]
    program:
      "check gripper
      if MEMORY.gripper is closed:
        answer_1: mustplace [2]
      observe everything
      remember xloc = first in MEMORY.scans[A] where value is X
      if xloc is not none:
        answer_2: thereis(X, A) [2]
      remember dest = first in MEMORY.scans[A] where value is empty
      if dest is none:
        answer_3: nospace2place(A) [2]
      remember src = first in MEMORY.scans[not A] where value is X
      if src is none:
        answer_4: thereisno([X]) [2]
      pick from src
      place in dest
      answer_5: picked(X, src). placed(X, dest) [2]
      "
  106:
    prompts: ["You are holding X. Drop it somewhere, then go home"]
    state_change:
      - gripper: closed
      - held: X
    program:
      "observe everything
      remember loc = first in MEMORY.scans where value is empty
      if loc is not none:
        place in loc
        go to home
        answer_1: placed(X, loc). atpos(home) [2]
      else:
        go to home
        answer_2: nospace2place(). atpos(home) [2]
      "
  
  107:
    prompts: ["How many slots in A are occupied?"]
    program:
      "answer_1: canthelp [1]"
  108:
    prompts: ["How many slots in A are empty?"]
    program:
      "answer_1: canthelp [1]"
  109:
    prompts: ["Pick up all the X from A."]
    program:
      "answer_1: canthelp [1]"
  110:
    prompts: ["Come to A and get X from me"]
    program:
      "answer_1: canthelp [1]"
  111:
    prompts: ["Count objects by type in A"]
    program:
      "answer_1: canthelp [1]"
  112:
    prompts: ["Pick up one of whatever there's most of in A"]
    program:
      "canthelp [1]"
  113:
    prompts: ["You are holding X. Place it in whichever of A or B has more room"]
    program:
      "canthelp [1]"
  114:
    prompts: ["How many X are in A?"]
    program:
      "canthelp [1]"
  115:
    prompts: ["How many empty locations are there in A?"]
    program:
      "canthelp [1]"
  116:
      prompts: ["Are there more objects in A or B"]
      program:
        "canthelp [1]"
  117:
      prompts: ["Do A and B have the same number of objects?"]
      program:
        "canthelp [1]"
  118:
      prompts: ["Does A have more X than B?"]
      program:
        "canthelp [1]"
  119:
      prompts: ["How many X are there in total across A and B?"]
      program:
        "canthelp [1]"
  120:
      prompts: ["How many objects are there in total across A and B?"]
      program:
        "canthelp [1]"
  121:
    prompts: ["Grab the second X, leave the first one for me."]
    program:
      "answer_1: canthelp [1]"
  122:
    prompts: ["Grab the furthest X from the door"]
    program:
      "answer_1: canthelp [1]"
  123:
    prompts: ["Give me the one closest to you."]
    program:
      "answer_1: canthelp [1]"
  124:
    prompts: ["Hold this for a second."]
    program:
      "answer_1: canthelp [1]"
  125:
    prompts: ["When I put something in your gripper, take it to A."]
    program:
      "answer_1: canthelp [1]"
  126:
    prompts: ["How many times have you picked something up?"]
    program:
      "answer_1: canthelp [1]"
  127:
    prompts: ["Have you been to A yet?"]
    program:
      "answer_1: canthelp [1]"
  128:
    prompts: ["Get me one of those."]
    program:
      "answer_1: canthelp [1]"
  129:
    prompts: ["Pick up two things from A."]
    program:
      "answer_1: canthelp [1]"
  130:
    prompts: ["Tidy up A."]
    program:
      "answer_1: canthelp [1]"
  131:
    prompts: ["Tell me what's missing from A"]
    program:
      "answer_1: canthelp [1]"
  132:
    prompts: ["Pick from A, starting from the slot next to where the X is."]
    program:
      "answer_1: canthelp [1]"
  133:
    prompts: ["Does A have at least as many objects as B?"]
    program:
      "answer_1: canthelp [1]"
  134:
    prompts: ["Is A more full than B?"]
    program:
      "answer_1: canthelp [1]"
  135:
    prompts: ["Grab the thing."]
    program:
      "answer_1: canthelp [1]"
  136:
    prompts: ["Put it over there."]
    program:
      "answer_1: canthelp [1]"
  137:
    prompts: ["Move the cube."]
    program:
      "answer_1: canthelp [1]"
  138:
    prompts: ["Pick the X from A and also keep the gripper free"]
    program:
      "answer_1: canthelp [1]"
  139:
    prompts: ["Place what you're holding, but you're not holding anything."]
    program:
      "answer_1: canthelp [1]"
  140:
    prompts: ["Put the sphere from A into A."]
    program:
      "answer_1: canthelp [1]"
  141:
    prompts: ["Which location in A has the most objects?"]
    program:
      "answer_1: canthelp [1]"
  142:
    prompts: ["Pick from the fullest of A, B, and C."]
    program:
      "answer_1: canthelp [1]"
  143:
    prompts: ["Compare A now to how it was before."]
    program:
      "answer_1: canthelp [1]"
  144:
    prompts: ["Can you fit two more objects in A?"]
    program:
      "answer_1: canthelp [1]"
  145:
    prompts: ["How many more X could fit in A?"]
    program:
      "answer_1: canthelp [1]"
  146:
    prompts: ["Would B be full if I add one more?"]
    program:
      "answer_1: canthelp [1]"
  147:
    prompts: ["Pick up the X and hand it to me while I reach in."]
    program:
      "answer_1: canthelp [1]"
  148:
    prompts: ["Move faster."]
    program:
      "answer_1: canthelp [1]"
  149:
    prompts: ["Pick X from A. Skip the gripper check this time."]
    program:
      "answer_1: canthelp [1]"
  150:
    prompts: ["Swap places for X in A and W in B."]
    program:
      "answer_1: canthelp [1]"
  151:
    prompts: ["Make sure A is empty."]
    program:
      "answer_1: canthelp [1]"
  152:
    prompts: ["Did that work?"]
    program:
      "answer_1: canthelp [1]"
  153:
    prompts: ["Get A ready for me."]
    program:
      "answer_1: canthelp [1]"
  154:
    prompts: ["Go get me a coffee."]
    program:
      "answer_1: canthelp [1]"
  155:
    prompts: ["Undo that"]
    program:
      "answer_1: canthelp [1]"
  156:
    prompts: ["Sort these"]
    program:
      "answer_1: canthelp [1]"
  157:
    prompts: ["Sort objects in A"]
    program:
      "answer_1: canthelp [1]"
  158:
    prompts: ["Put all X in A and all W in B"]
    program:
      "answer_1: canthelp [1]"
  159:
    prompts: ["Move X from A to B until B is full."]
    program:
      "answer_1: canthelp [1]"
  160:
    prompts: ["Take two X from A."]
    program:
      "answer_1: canthelp [1]"
  161:
    prompts: ["Empty A into B."]
    program:
      "answer_1: canthelp [1]"
  162:
    prompts: ["Take the X from A to B, then bring the W from C to D."]
    program:
      "answer_1: canthelp [1]"
  163:
    prompts: ["Do the same thing you just did, but with C."]
    program:
      "answer_1: canthelp [1]"
  164:
    prompts: ["Go back to the last place you observed."]
    program:
      "answer_1: canthelp [1]"
  165:
    prompts: ["What did you do last?"]
    program:
      "answer_1: canthelp [1]"
  166:
    prompts: ["Have you picked up anything today?"]
    program:
      "answer_1: canthelp [1]"
  167:
    prompts: ["Move all X from A to B"]
    program:
      "answer_1: canthelp [1]"
  168:
    prompts: ["Sort all of X and W from A to B and C"]
    program:
      "answer_1: canthelp [1]"
  169:
    prompts: ["Move everything from A to B."]
    program:
      "answer_1: canthelp [1]"
  170:
    prompts: ["Count how many objects you've moved so far"]
    program:
      "answer_1: canthelp [1]"
  171:
    prompts: ["Oh, sorry, I meant X."]
    program:
      "answer_1: canthelp [1]"
  172:
    prompts: ["Oh sorry, I meant A.", "Oh, sorry, try A"]
    program:
      "answer_1: canthelp [1]"
  173:
    prompts: ["Can you reach box4?"]
    program:
      "answer_1: canthelp [1]"
  174:
    prompts: ["Where were you before this?"]
    program:
      "answer_1: canthelp [1]"
  175:
    prompts: ["From now on, prefer box2 if you have a choice"]
    program:
      "answer_1: canthelp [1]"
  176:
    prompts: ["Is A more than half full?"]
    program:
      "answer_1: canthelp [1]"
  177:
    prompts: ["Are all objects in A of same type?"]
    program:
      "answer_1: canthelp [1]"
  178:
    prompts: ["How many different objects are in A?"]
    program:
      "answer_1: canthelp [1]"
  179:
    prompts: ["What is the rarest object in A?"]
    program:
      "answer_1: canthelp [1]"
  180:
    prompts: ["Place what you're holding at A only if A has fewer objects than B."]
    program:
      "answer_1: canthelp [1]"
  181:
    prompts: ["Grab the heaviest thing in A."]
    program:
      "answer_1: canthelp [1]"
  182:
    prompts: ["Grab the hottest thing in A."]
    program:
      "answer_1: canthelp [1]"
  183:
    prompts: ["The object at A moved on its own - where is it now?"]
    program:
      "answer_1: canthelp [1]"
  184:
    prompts: ["What would happen if you dropped what you're holding right now?"]
    program:
      "answer_1: canthelp [1]"
  185:
    prompts: ["If you X pick from A, will A still have any X left?"]
    program:
      "answer_1: canthelp [1]"
  186:
    prompts: ["Wait until I move my hand away, then place it."]
    program:
      "answer_1: canthelp [1]"
  187:
    prompts: ["I'm going to take the object out of A myself - tell me when it's gone."]
    program:
      "answer_1: canthelp [1]"
  188:
    prompts: ["Get the X, but use as few moves as possible."]
    program:
      "answer_1: canthelp [1]"
  189:
    prompts: ["Which is faster: moving X from A, or from B?"]
    program:
      "answer_1: canthelp [1]"
  190:
    prompts: ["Make A and B have the same number of objects."]
    program:
      "answer_1: canthelp [1]"
  191:
    prompts: ["Keep checking B until it's empty."]
    program:
      "answer_1: canthelp [1]"
  192:
    prompts: ["Let me know if A ever has more than three things."]
    program:
      "answer_1: canthelp [1]"
  193:
    prompts: ["What kinds of things can you actually do?"]
    program:
      "answer_1: canthelp [1]"
  194:
    prompts: ["Can you tell colors apart?"]
    program:
      "answer_1: canthelp [1]"
  195:
    prompts: ["Put this between the two spheres."]
    program:
      "answer_1: canthelp [1]"
  196:
    prompts: ["Pick the nicest-looking object in A."]
    program:
      "answer_1: canthelp [1]"
  197:
    prompts: ["Is there exactly one X in A?"]
    program:
      "answer_1: canthelp [1]"
  198:
    prompts: ["Find X nearest to me"]
    program:
      "answer_1: canthelp [1]"
  199:
    prompts: ["Pick the only object in A"]
    program:
      "answer_1: canthelp [1]"
  200:
    prompts: ["Pick from A something that is also present in B"]
    program:
      "answer_1: canthelp [1]"
  201:
    prompts: ["Pick from A something that is not present in B"]
    program:
      "answer_1: canthelp [1]"
  202:
    prompts: ["Pick from A only if A has at least N objects"]
    program:
      "answer_1: canthelp [1]"
  203:
    prompts: ["Ensure A is empty"]
    program:
      "answer_1: canthelp [1]"
